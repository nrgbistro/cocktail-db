import SearchBar from "./search/components/SearchBar";

const BasicSearch = () => {
	return (
		<div className="w-[50%] bg-red-500 min-h-5 h-5">
			<SearchBar />
		</div>
	);
};

export default BasicSearch;
